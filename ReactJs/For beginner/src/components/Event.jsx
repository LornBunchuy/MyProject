import React from "react";

const Event = () => {
  //   const handleClick = (e) => (e.target.textContent = "Hello baby!");
  //   return <button onClick={(e) => handleClick(e)}>Click me please</button>;
};

export default Event;
